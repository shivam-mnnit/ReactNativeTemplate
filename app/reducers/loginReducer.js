/**
 * Created by saionara1 on 6/21/17.
 */
const initialState = {};
export default function loginReducer(state = initialState, action = {}) {
    switch (action.type) {
        default:
            return state;
    }
}